Absolute → Yes, you can move anywhere, but it does overwrite/overlap others because it’s removed from flow.

Relative → You can move anywhere, but it keeps its original space, so overlap happens.

Static → Default, no shifting allowed.

Sticky → Moves with scroll until it reaches the defined edge, then sticks.

Fixed → Always fixed to viewport, regardless of scroll.